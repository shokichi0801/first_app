<h1>トップページ</h1>
<%= link_to '投稿ページ', 'post/new' %>
<%= @post .each do |post|%>
<div>
<%= post.memo%>
<%= post.created_at %>
</div>
<% end%>

<%= form_with url: '/posts', method: :post, local: true do |form| %>
  <%= form.text_field :memo %>
  <%= form.submit "投稿する" %>
<% end %>